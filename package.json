{
  "name": "larry.engineer",
  "version": "1.0.0",
  "author": "larry <gm@larry.engineer>",
  "license": "UNLICENSED",
  "scripts": {
    "build": "rm -rfv dist && npx parcel build src/index.html --public-url /",
    "build-votes": "rm -rfv dist-votes && npx parcel build src/vote*.html --public-url /votes --dist-dir dist-votes",
    "serve": "npx parcel src/index.html"
  },
  "dependencies": {
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.31.0",
    "@typescript-eslint/parser": "^5.31.0",
    "eslint": "^8.20.0",
    "parcel": "^2.6.2",
    "typescript": "^4.7.4"
  },
  "engines": {
    "node": "^18"
  }
}
